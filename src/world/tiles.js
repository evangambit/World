/**
 * Tile type constants and gameplay rules.
 * @see tileTypes.js — same exports (this file re-exports for backward compatibility).
 * @see ../client/tileArt.js — sprite pre-rendering (getTileCanvas, getObjCanvas).
 */
export * from './tileTypes.js';
